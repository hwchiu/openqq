apiVersion: security.kubearmor.com/v1
kind: KubeArmorPolicy
metadata:
  name: audit-etc-nginx-access
  namespace: default
spec:
  selector:
    matchLabels:
      app: kubearmor-demo
  file:
    matchDirectories:
      - dir: /etc/nginx/
        recursive: true
  action: Audit
