apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opensandbox-server-data
  namespace: opensandbox-system
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
