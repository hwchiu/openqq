# Secrets & credentials
.env
*.env.local

# Terraform
*.tfvars
*.tfstate
*.tfstate.*
.terraform/
crash.log

# Azure CLI session
.azure/

# Generated cluster artifacts (kubeconfig, cluster.env, raw certs)
generated/
.artifacts/kubeconfig

# Kubeconfig files that may appear at repo root
kubeconfig
kubeconfig.raw
*.kubeconfig

# Tenant Server local build artifact
opensandbox-demo/tenant-server-go/tenant-server-linux-amd64

# Local comparison-matrix runner outputs
/testing/results/latest/*
!/testing/results/latest/README.md
